import { TextInput } from '@mantine/core';
/**
 * This is a stub - no actual filtering logic for this minimal sample
 */
export function Filter() {
  return <TextInput placeholder="Filter items..." disabled />;
}